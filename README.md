# Nox

Nox is a Unix-like operating system built around the **Twilight** kernel.

The repository is currently at the earliest bring-up stage: Twilight boots on x86_64 through Limine, acquires a linear framebuffer, renders text directly into it, and halts.

See `docs/building.md` for macOS and Linux build instructions.
